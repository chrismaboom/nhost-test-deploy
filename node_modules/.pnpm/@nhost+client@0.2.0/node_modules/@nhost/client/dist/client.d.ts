import { InterpreterFrom } from 'xstate';
import { NhostMachine, NhostMachineOptions } from './machines';
export declare type NhostClientOptions = NhostMachineOptions;
export declare class Nhost {
    #private;
    readonly backendUrl: string;
    readonly clientUrl: string;
    readonly machine: NhostMachine;
    interpreter?: InterpreterFrom<NhostMachine>;
    constructor({ backendUrl, clientUrl, storageGetter, storageSetter, autoSignIn, autoRefreshToken }: NhostClientOptions);
}
