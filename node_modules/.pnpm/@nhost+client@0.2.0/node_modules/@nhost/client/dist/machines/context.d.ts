import { ErrorPayload } from '../errors';
import { User } from '../types';
export declare type NhostContext = {
    user: User | null;
    mfa: boolean;
    accessToken: {
        value: string | null;
        expiresAt: Date;
    };
    refreshTimer: {
        elapsed: number;
        attempts: number;
    };
    refreshToken: {
        value: string | null;
    };
    errors: Partial<Record<'registration' | 'authentication', ErrorPayload>>;
};
export declare const INITIAL_MACHINE_CONTEXT: NhostContext;
